<!DOCTYPE html>
<html lang="{{ page.lang }}">

    <!-- HTML head, it contains style files and site information. -->
    <head>
        {{ partial("_partials/head.njk", {}, {cache: theme.cache.enable}) }}
        <title>{{ config.title }}</title>
    </head>

    <body>

        <!-- Image preloader for faster image loading (TODO). -->
        <div class="image-preloader"></div>

        <!-- Layout container. Used to isolate main content from other div objects. -->
        <div class="mdl-layout__container">
            <!-- No header, and the drawer stays open on larger screens.
                 We use this fixed drawer for convience and better mobile performance. -->
            <div class="mdl-layout mdl-js-layout mdl-layout--fixed-drawer site-layout">
                
                <!-- Drawer layout. This part can be seen as "header". -->
                <div class="mdl-layout__drawer site-drawer">

                    <!-- Site title in drawer. -->
                    <span class="mdl-layout-title">
                        {{ config.title }}
                    </span>

                    <!-- Site navigation. Will be defined in navigation.njk (TODO). -->
                    <nav class="mdl-navigation">
                        <!-- Home link. -->
                        <a class="mdl-navigation__link" href="/">
                            <i class="material-icons mdl-list__item-icon">home</i>
                            Home
                        </a>
                        <!-- Archives link. -->
                        <a class="mdl-navigation__link" href="/archives">
                            <i class="material-icons mdl-list__item-icon">archives</i>
                            Archives
                        </a>
                        <!-- Link. -->
                        <a class="mdl-navigation__link" href="">
                            <i class="material-icons">link</i>
                            Link
                        </a>
                        <!-- Link. -->
                        <a class="mdl-navigation__link" href="">
                            <i class="material-icons">link</i>
                            Link
                        </a>
                    </nav>
                    
                </div>

                <!-- Main content, extended in specific pages. -->
                <main class="mdl-layout__content content__container">
                    {% block content %}{% endblock %}
                </main>


            </div>
        </div>
    </body>

</html>